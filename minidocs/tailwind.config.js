// /** @type {import('tailwindcss').Config} */
// export const content = [
//   "./src/**/*.{js,jsx}",
// ];
// export const theme = {
//   extend: {},
// };
// export const plugins = [];

// tailwind.config.js

// tailwind.config.js

const config = {
  purge: ["./src/**/*.{js,jsx}"], // Specify files to purge
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {}, // Extend Tailwind CSS theme
  },
  variants: {
    extend: {}, // Extend Tailwind CSS variants
  },
  plugins: [], // Include Tailwind CSS plugins
};

export default config;



